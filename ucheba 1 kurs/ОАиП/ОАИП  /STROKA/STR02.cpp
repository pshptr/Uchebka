#include <stdio.h>
#include <alloc.h>
#define size 2
void fff(char *,char *);
// ???? ????? ???,
// ?????? ????? ?????? ?? ??? ??? ?????? ????
// ?????? ?? ?????? ???? c???? ? ???????? ???????
void main()
{ char *st[size][10],*mst,*pst;
  int i,j,n=0,k=0,kk=0;
  n=0;
  do
  { st[n][0]=(char *)malloc(20);
    gets(st[n][0]);                   // ???? ????
  } while(*st[n++][0]);
  n--;

  for(i=0;i<n;i++)
  { k=0;
    for(j=0; *(st[i][0]+j); j++)
    if(*(st[i][0]+j)==' ')     // ????? ??? ? ????
      st[i][++k]=st[i][0]+j+1; // ???? ???? ???
    st[i][++k]=st[i][0]+j+1;   // ???? \0 (??? ???????? ???)
    for(; k<9; )
    st[i][++k]=NULL;           // ????? ???? ??? ?? ? ????
  }
  for(i=0; i<n; i++)             // ????? ???
  for(j=0; st[i][j]; j++)        // ????? ?? ? ????
  fff(st[i][j],st[i][j+1]);    // ??????? 2 ?? ??? ???? max

  for(i=0; i<n; i++)
  puts(st[i][0]);
}

// void fff(char st1[],char st2[]) ???????? ???????? ????
void fff(char *st1,char *st2)
{  int i;
   static j;
   static char *k;
   j=(st2-st1>j) ? k=st1,j=st2-st1 : j;
	      // j - ????? ????????? ???
	      // k - ????? ?? ???? ??? ???
   if (!st2)          // ?????? ?? ???? ???
   { for(; j>0; j--)
     { for(i=0; *(k+i+1); i++)
       *(k+i)=*(k+i+1);     // ??? ???? ?? ?????? max ???
       *(k+i)='\0';
      }
    }
}

