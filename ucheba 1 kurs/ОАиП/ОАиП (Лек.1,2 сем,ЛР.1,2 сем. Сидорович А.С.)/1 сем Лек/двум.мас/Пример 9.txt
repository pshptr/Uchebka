
// naiti sum elementov pod glav diag matre

# include <stdio.h>
int main()
{
	int N, matr[100][100], i, j, S = 0, k;
	printf("Input size\n");
	scanf("%d", &N);

	for (i = 0; i < N; i++)
		for (j = 0; j < N; j++)
			scanf("%d", &matr[i][j]);

	for (i = 0; i < N; i++)
	{
		for (j = 0; j < N; j++)
			printf("%3d", matr[i][j]);
		printf("\n");
	}

	
	
	for (i = 1; i < N; i++)
	{
		for (j = 0; j < i; j++)
			S += matr[i][j];
		
	}


	printf("summ= %d", S);

		return 0;
}