#include <iostream>
#include <cmath>
using namespace std;

int main()
{
     double x1 = 3,a ;
      double y1 = 2,b ;
       double x2 = 3,c ;
          double y2 = 6,S ;
           double x3 = 5,p ;
            double y3 = 1 ;
   
   a = sqrt(pow(x2 - x1,2.0) + pow(y2 - y1,2.0));
   b = sqrt(pow(x3 - x2,2.0) + pow(y3 - y2,2.0));
   c = sqrt(pow(x3 - x1,2.0) + pow(y3 - y1,2.0));
 
  // S=sqrt(pow(x2-x1,2.0)+pow(y2-y1,2.0));
   
    p = (a + b + c) / 2;
    S = sqrt(p * (p - a) * (p - b) * (p - c));
    
    std::cout << S << std::endl;
    std::cout << p << std::endl;
  
    return 0;
}









#include <iostream>
#include "cmath"

using namespace std;

namespace part_1 {
     void begin1(){
      double a;
      cout << "Введите сторону квадрата: ";
      cin >> a;
      double p = 4 * a;
      cout << "Периметр квадрата = " << p << endl;
   }
 
  void begin2(){
      double a;
      cout << "Введите сторону квадрата: ";
      cin >> a;
      double s = pow(a, 2.0);
      cout << "Площадь квадрата = " << s << endl;
   }
 
  void begin3(){
      /*Даны две переменные целого типа: A и B. 
Если их значения не равны, 
то присвоить каждой переменной сумму этих значений, а если равны, то 
присвоить переменным нулевые значения. Вывести новые 
значения переменных A и B.*/
      int a,b,c;
      cout<<"Введите числа: ";
      cin>>a>>b;
      c=a+b;
      if (a!=b) { a=b; 
      } 
      if (a==b) { a=0; b=0;
      }
     cout<<a<<endl;
     cout<<b<<endl;
     cout<<c<<endl;
      
  }

 }
int main()
{
   part_1::begin3();
   return 0;
}



