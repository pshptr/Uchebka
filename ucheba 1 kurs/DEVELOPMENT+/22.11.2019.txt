#include <iostream>
#include <cmath>

using namespace std;

namespace part_1
{

  void begin1 ()
  {
    
    int x, y;
     cout << "¬ведите значени€ x и y "<<endl;
      cin >> x >> y;
     if ( x > 0 && y > 0 ) {
        cout<<"I"<<endl; 
     } else if ( x < 0 && y > 0 ) {
        cout<<"II"<<endl;  
     } else if ( x < 0 && y < 0 ) {
         cout<<"III"<<endl; 
     } else
     if ( x > 0 && y < 0 ) {
         cout<<"IV"<<endl; 
     }
  }

  void begin2 ()
  {
      /*ƒл€ данного целого x найти значение следующей функции f,
      принимаю-щей значени€ целого типа:
      2Јx, если x&lt; Ц2 или x> 2,  f(x) =  Ц3Јx, в противном случае.*/
    int x,f;
     cout<<"¬ведите значение х "<<endl;
    cin>>x;
    if ( x<-2 || x>2 ) {
      f=2*x;   
    } else f=-3*x; 
     
     cout<<"f(x) = " << f<<endl;  
    
   
  }
  
  void begin3 ()
  {
      /*ƒано  целое  число,  лежащее  в  диапазоне1Ц999.
      ¬ывести  его  строку-описание вида
      Ђчетное двузначное числої, Ђнечетное трехзначное числої  и т. д.*/
      int x;
      cin>>x;
      x>=1 && x<=99;
      
      if(x>=1 && x<=9) {
          if(x % 2 == 0 ) {
               cout<<x<<"четное однозначное число "<<endl;
          } else {
              cout<<x<<"нечетное однозначное число "<<endl; 
          }
      } e
      
      if(x>=1 && x<=9 && x % 2 == 0 ) {
         cout<<x<<"четное однозначное число "<<endl; } 
          
         else if(x>=1 && x<=9 && x % 2 != 0 ) {
         
         cout<<x<<"нечетное однозначное число "<<endl; 
      }
      else if(x>=10 && x<=99 && x % 2 == 0 ) {
          
          cout<<x<<"четное двузначное число "<<endl;
      } 
      else if(x>=10 && x<=99 && x % 2 != 0 )
     
     { cout<<x<<"четное двузначное число "<<endl; }
  }
}
int main ()
{
  part_1::begin3 ();
  return 0;
}
